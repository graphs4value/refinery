plugins {
	id 'application'
	id 'com.github.johnrengelman.shadow'
	id 'refinery-java-conventions'
}

for (taskName in ['distTar', 'distZip', 'shadowDistTar', 'shadowDistZip']) {
	tasks.named(taskName) {
		enabled = false
	}
}
