const PREFIX = 'problem-editor-';

const editorClassNames = {
  foldPlaceholder: `${PREFIX}fold-placeholder`,
  foldMarker: `${PREFIX}fold-marker`,
  foldMarkerClosed: `${PREFIX}fold-marker-closed`,
  foldMarkerOpen: `${PREFIX}fold-marker-open`,
};

export default editorClassNames;
